import 'package:flutter/material.dart';

class AddWatchedEpisodeScreen extends StatelessWidget {
  const AddWatchedEpisodeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("AddWatchedEpisodeScreen"),
      ),
    );
  }
}
